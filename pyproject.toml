[project]
name = "terminal-starship"
version = "0.1.0"
description = "Real-time ANSI orbital control system"
authors = [{name = "Artemiya"}]
requires-python = ">=3.8"

[project.scripts]
terminal-starship = "main:main"

[tool.setuptools.packages.find]
where = ["src"]