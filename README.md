# Terminal-Starship
Real-time ASCII orbital control system

## Features
- Real-time ASCII animation in terminal
- Signal handling for graceful shutdown:
  - Ctrl+C (SIGINT) - exits gracefully
  - Ctrl+D (EOF) - exits gracefully

## Build
```
pip install -e .
terminal-starship
```